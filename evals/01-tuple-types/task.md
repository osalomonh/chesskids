Use the move-logic agent. In `moves.ts`, define `type Offset = readonly [number, number]` and annotate all five offset and direction constants as `readonly Offset[]`. Update the `offsets` and `directions` parameters on `jumpMoves` and `slideMoves` to match. Do not use non-null assertions and do not change any tsconfig setting. No behaviour change: `npm run check` must exit clean and `npm test` must still pass.